<div class="container">
  <div class="grid">
    <div class="col">
      <br/>
      <div class="product-content">
        <%= render 'plantform', product: @plant %>
      </div>
    </div>

    <div class="card-variant-L">
      <div class="card padd col">
        <%= render 'cardvariant', variant: @plant.givevariant("L"), format: "L", addr: "Adopte1plante/addvariantL.png" %>
      </div>
    </div>

    <div class="card-variant-M">
      <div class="card padd col">
        <%= render 'cardvariant', variant: @plant.givevariant("M"), format: "M", addr: "Adopte1plante/addvariantL.png" %>
      </div>
    </div>

    <div class="card-variant-S">
      <div class="card padd col">
        <%= render 'cardvariant', variant: @plant.givevariant("S"), format: "S", addr: "Adopte1plante/addvariantL.png" %>
      </div>
    </div>

  </div>
</div>


